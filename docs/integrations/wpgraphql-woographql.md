# WPGraphQL and WooGraphQL Integration

The GraphQL adapter placeholder lives at `src/lib/woocommerce/graphql-client.ts`.

Potential uses:
- Content-rich product fields
- SEO descriptions
- Category and tag trees
- COA relationship fields

The internal `/api/graphql` route currently exposes mock provider data and should remain provider-backed.
